# Development config
version: '3'
services:
  web:
    image: cb-flask:dev
    build:
      context: ./web
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - './web/:/home/app'
    env_file:
      - secrets.list
  redis:
    image: "redis:alpine"
